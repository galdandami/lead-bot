name: Keep alive

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend (PythonAnywhere / Render)
        run: curl -sS -o /dev/null -w "%{http_code}\n" "${{ secrets.LEAD_BOT_URL }}/"
      - name: Ping Supabase REST API (public read, counts as activity)
        run: curl -sS -o /dev/null -w "%{http_code}\n" -H "apikey: sb_publishable_594WMbRbwJNRKmURHKPK-A_ZjvklqWO" -H "Authorization: Bearer sb_publishable_594WMbRbwJNRKmURHKPK-A_ZjvklqWO" "https://udozteluyreuhfdgjiqq.supabase.co/rest/v1/site_content?select=*&limit=1"
